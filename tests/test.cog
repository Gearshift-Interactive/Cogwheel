var numbers1 = new [ 1, 2, 3, 4, 5, 6 ];
// var numbers2 = new [ 7, 8, 9, 10, 11, 12 ];
var numbers = numbers1 * 2u;

mut var i = 0u;
while (i < sizeof numbers)
{
    putInt((int)i);
    putChar('-');
    putInt(numbers[i]);
    putChar('\n');
    i = i + 1u;
};
